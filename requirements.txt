# Core RAG dependencies (all open source)
llama-index==0.10.12
llama-index-llms-ollama==0.1.3
llama-index-embeddings-huggingface==0.2.0
llama-index-vector-stores-chroma==0.1.6

# Vector database (free, local)
chromadb==0.4.22

# Embedding models (free, local)
sentence-transformers>=2.6.1

# UI (free, local)
gradio==4.16.0

# Optional: If you want to use Claude API instead
# llama-index-llms-anthropic==0.1.14
